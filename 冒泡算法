#冒泡算法*
## 1 ：**文字**
介绍：冒泡排序是一种简单的排序算法。这个算法的名字的由来是因为越小的元素会经由交换慢慢“浮”到数列的顶端。
逻辑：遍历数组，比较两个相邻的元素，如果前一个比后一个大，那么就把这两个互换位置。数组遍历一遍之后，最后一个元素就是最大的数字。然后在进行遍历，按照之前的规则，最后倒数第二元素是第二大的数字。以此类推，按照从小到大的顺序把数组排列好。
## 2 ：**代码**
#include <iostream>
using namespace std;
int main()
{
   int arr[10]={10,2,45,3,6,22,67,24,56,90}
   int i,j;
   int temp=0;
   for(i=0;i<9;i++)
   {
      for(j=0;j<9-i;j++)
      {
         if(arr[j]>arr[j+1])
         {
            temp=arr[j];
            arr[j]=arr[j+1];
            arr[j+1]=temp;
         }
      }
   }
   cout<<"排序后:"<<endl;
   for(i=0;i<10,i+)
   {
      cout<<arr[i]<<" ";
   }
   cout<<endl;

   return 0;
}
## 3 ：**时间复杂度**
最优时间复杂度：O(n)（表示遍历一次发现没有任何可以交换的元素，排序结束。）
最坏时间复杂度：O(n2)
稳定性：稳定
## 4 ：**图片**
